spring:
  application:
    name: statusmonitor
  profiles:
    active: dev

  monitoring:
    check-interval: 30000
    connection-timeout: 5000
    read-timeout: 30000
    max-retries: 2
    retry-delay: 1000
    user-agent: StatusMonitor/1.0